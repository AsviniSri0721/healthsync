apiVersion: v1
kind: Service
metadata:
  name: {{ include "healthsync.fullname" . }}
  labels:
    {{- include "healthsync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "healthsync.selectorLabels" . | nindent 4 }}
