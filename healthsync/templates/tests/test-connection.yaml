apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthsync.fullname" . }}-test-connection"
  labels:
    {{- include "healthsync.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "healthsync.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
