{
  "extends": "../../tsconfig.json", // Assuming there's a root-level tsconfig.json
  "compilerOptions": {
    "outDir": "./", // Ensure this points to the right directory
    "target": "ES2020",
    "module": "CommonJS",
    "strict": true,
    "esModuleInterop": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
